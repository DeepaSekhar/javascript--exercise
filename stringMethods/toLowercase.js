//makes the string all lowercase letters
var string1 = "MY NAME IS DEEPA";
const toLowerCase = (str1) => {
  return str1.toLowerCase();
};
console.log(toLowerCase(string1));

// o/p=>my name is deepa
