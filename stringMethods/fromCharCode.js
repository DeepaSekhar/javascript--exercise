// It returns the character code of the specified character in a string

// const fromCharCode = (str) => {
//   return String.fromCharCode(str);
// };
// console.log(fromCharCode("72, 69, 76, 76, 79"));
// Only can pass one number because its a string
// If we want to pass it as a word covert into array

const text = String.fromCharCode(72, 69, 76, 76, 79);
console.log(text);
